import React from "react";
import styled from "styled-components";

export const Home = () => {
  return <HomeContainer>This is the home page</HomeContainer>;
};

export default Home;

const HomeContainer = styled.section``;
